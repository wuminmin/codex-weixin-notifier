{
  "name": "codex-weixin-notifier",
  "version": "0.2.0",
  "description": "Send Weixin and Feishu notifications and route isolated Codex tasks from multiple bots.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.71.1",
    "@napi-rs/canvas": "^1.0.3",
    "marked": "^18.0.5",
    "qrcode-terminal": "^0.12.0"
  }
}
